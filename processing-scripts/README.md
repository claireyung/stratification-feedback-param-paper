# processing-scripts

Processing scripts for compressing data for public upload

- Using `ncra` to take means over study period (last 180 days of ISOMIP experiments)
- Copying output to new directory and deleting the big files, leaving just small files, grid information and compressed data
